/// The `@Throws` annotation for the `checked_exceptions` analyzer plugin.
///
/// Add `checked_exceptions` to `dependencies` to use the annotation, and to
/// the top-level `plugins` section of `analysis_options.yaml` to enable the
/// rules that act on it.
library;

export 'package:checked_exceptions/src/annotations.dart' show Throws;
